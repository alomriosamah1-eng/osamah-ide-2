# مراجعة تفاعلات مساحة العمل

## لوحة الأوامر

فُتحت لوحة الأوامر من الجلسة المحلية المصادَق عليها باستخدام زر `Ctrl K`. ظهرت ثلاثة أوامر فقط، وكلها وجهات مساحة عمل فعلية وليست وعوداً بتوليد أو محادثة غير مهيأة: **البرمجة** و**استوديو العروض** و**العقل الثاني**. ظهر حقل بحث يعمل بعقد إمكانية وصول `combobox` وقائمة نتائج `listbox`، مع خيار نشط وتعليمات صادقة للتنفيذ والتنقل بلوحة المفاتيح.

> لا تتضمن اللوحة أي أمر لإنشاء محتوى مولّد أو فتح محادثة OpenCode دون نموذج متاح. يبقى إرسال الوكيل معطلاً كما يقرر فحص الصحة الخادمي.

## نتيجة التنفيذ

أُدخلت عبارة **«البرمجة»** في الحقل، فاقتصرت القائمة على أمر «فتح مساحة البرمجة». نُفّذ الأمر بزر `Enter` وانتقلت الجلسة فعلياً إلى قسم البرمجة؛ اختفت اللوحة وظهر مستكشف المشاريع الخالي وحالة Theia الصادقة التي تفيد بأن مصدره المضمّن موجود لكن ناتج تطبيق المتصفح غير مبني. بذلك أصبح الحقل والترشيح و`Enter` مؤثرات وظيفية قابلة للتحقق، لا نصاً تجميلياً.

## مركز المساعدة السياقي

فُتح مركز المساعدة من الزر ذي الصنف `.help-center-trigger` في جلسة تطوير محلية مصادَق عليها بتاريخ 26 أغسطس 2026. ظهرت اللوحة العربية باتجاه RTL بعنوان **«مساعدة مرتبطة بمساحة العمل»**، وبها عبارة صريحة بأن المعلومات تقرأ حالة المحرك وبيانات مساحة العمل الحالية ولا تعرض إجراءات تجريبية. لا تعتمد الصفوف على بيانات ثابتة؛ بل تستمد طورها من استعلام `engines.status`، وتتعامل الواجهة قبل وصوله مع التحميل، ثم الخطأ، ثم فراغ البيانات، ثم البيانات الجاهزة بواسطة `getWorkspaceHelpEnginePhase`.

| المحرك المعروض | الحالة المرئية في الفحص الحي | الدلالة التشغيلية الصادقة |
|---|---|---|
| OpenCode | **جاهز** | المحرك المضمّن سليم، ويمكن بدء جلسة للمستخدم المصادَق عليه بعد اختيار نموذج مكتشف. |
| Eclipse Theia | **يتطلب بناء التطبيق** | المصدر موجود، لكن تطبيق المتصفح غير مبني؛ لا تدّعي اللوحة أن محرراً يعمل. |
| Presenton | **يتطلب إعداداً** | فحص الصحة متاح، لكن التوليد والتصدير لا يظهران كعمليات متاحة قبل إعداد مزود معتمد. |
| العقل الثاني | **متاح** | مهايئ المعرفة الخادمي ومستخرج المهام متاحان، ولا يُعرض توليد محتوى مصطنع. |

كما عرضت اللوحة روابط فعلية إلى **البرمجة** و**العروض التقديمية** و**العقل الثاني** و**الإعدادات**، مع اختصارات لوحة المفاتيح. يثبت اختبار الوحدة `pnpm exec vitest run client/src/lib/workspace-help.test.ts` أولوية حالة التحميل وتمييز حالات الخطأ والفراغ والجاهزية.

## اختيار النموذج ودردشة الوكيل

أظهر فحص الواجهة الحية أن قائمة النماذج قابلة للفتح وتحتوي نماذج مكتشفة من OpenCode. كان زر الإرسال معطلاً قبل إدخال رسالة لا بسبب فشل الصحة أو قائمة فارغة؛ لذلك استُبدلت العبارة المضللة **«بانتظار نماذج OpenCode»** بعد الاكتشاف بعبارة **«اختر نموذج OpenCode»**. لا يوجد نموذج افتراضي. يثبت اختبار `pnpm exec vitest run client/src/lib/opencode-contract.test.ts` الفرق بين انتظار اكتشاف نموذج ووجوب اختيار نموذج متاح.

اختير نموذج مكتشف صراحة ثم أُرسلت رسالة تحقق عربية قصيرة في الجلسة المصادَق عليها. عادت استجابة المساعد النصية **«متصل»** عبر جلسة OpenCode حقيقية، من دون تسجيل معرّف جلسة أو كشف سر أو تضمين بديل محلي. بذلك يطابق سلوك الواجهة العقد: لا إرسال مع مسودة فارغة أو بلا اختيار، وإرسال حي بعد صحة المحرك واختيار المستخدم.

في إعادة الفحص المرئي لاحقاً، ظهرت عبارة **«اختر نموذج OpenCode»** في حقل الاختيار وكان قابلاً للتفاعل، وعرضت الصفحة قائمة نماذج مكتشفة حية. لم تكن توجد جلسة نشطة في تلك اللحظة، لذا لم يظهر زر إنهاء الجلسة؛ وهذا مقصود لأن الزر لا يُعرض قبل إنشاء جلسة حقيقية مرتبطة بالحساب المحلي.

### دورة الجلسة المملوكة والتنظيف

اختير نموذج مكتشف من الواجهة وأُرسلت رسالة تحقق قصيرة. عند قبول الرسالة ظهر زر **«إنهاء الجلسة»** من دون عرض أي معرّف جلسة، ثم وصلت استجابة المساعد النصية. استُخدم الزر بعد ذلك؛ فاختفى من اللوحة وظهرت الرسالة الصادقة **«أُغلقت جلسة OpenCode وحُذفت من المحرك.»**. هذا يثبت مسار الإنشاء والرد والإزالة في الواجهة، بينما يثبت اختبار ملكية الوحدة أن حساباً مختلفاً لا يطابق الجلسة ولا يمكنه تحريرها.

### حاجز فشل التنظيف

لا يكفي بدء الإزالة وحده لحماية سياق القسم. لذلك صار فشل حذف جلسة القسم السابق حاجزاً مرئياً وصريحاً: تُعطّل أزرار الإرسال والاقتراحات وتبديل النموذج وفحص المحرك، وتعرض الواجهة سبب التعطيل بدلاً من إعادة استخدام معرّف الجلسة في قسم جديد. يبقى زر **«إنهاء الجلسة»** متاحاً لإعادة المحاولة؛ وعند نجاحها فقط تمسح الواجهة الحالة المحلية وتسمح بسياق جديد. يثبت اختبار `opencode-session.test.ts` الحالات الثلاث: وجود جلسة مع فشل التنظيف يحظر الإرسال، أما غياب الجلسة أو نجاح التنظيف فلا يحجبه.

اجتازت الدفعة بعد هذا التغيير `pnpm check`، ومجموعة Vitest الكاملة (**23 ملفاً و54 اختباراً**)، والتحقق الطرفي لمساحة العمل مع تنظيف بيانات الاختبار، وفاحصي جلسة OpenCode والأدوار الثلاثة والمهلة. لا تحفظ هذه الفحوص معرّفات جلسات أو مفاتيح مزود.

### موافقات OpenCode المتأخرة

تعرض لوحة الوكيل طلبات الصلاحية التي يعيدها الخادم مع الإرسال، وتستطلع الآن مسار الصلاحيات المملوك للجلسة كل أربع ثوانٍ أثناء انتظار رد OpenCode. لا يبدأ الاستطلاع بلا معرّف جلسة، ولا يستمر بعد اكتمال الرد؛ لذلك لا يمكنه طلب صلاحيات جلسة أخرى من المتصفح. تبقى أزرار **«سماح مرة»** و**«السماح دائماً»** و**«رفض»** مرتبطة برد الخادم المالك للجلسة، ويمنع وجود طلب معلق إرسال سياق جديد قبل القرار.

في الفحص الحي الحالي لم يُصدر النموذج المختار طلب صلاحية، ولذلك كانت بطاقات الصلاحية الظاهرة **صفر**؛ لم يُنشأ طلب اصطناعي لأجل العرض. تغطي اختبارات `opencode-session.test.ts` بدء الاستطلاع لجلسة منتظرة فقط، واستبدال القائمة بالاستجابة الفارغة، وإزالة الطلب بعد نجاح الرد، والاحتفاظ بالقرار الظاهر عند غياب استجابة الاستطلاع بسبب خطأ. اجتازت مجموعة Vitest الكاملة بعد الإضافة (**23 ملفاً و56 اختباراً**) مع `pnpm check` وفحص حدود التكامل الذي يؤكد بقاء OmniRoute خارج مسارات التشغيل.

## حماية التعديلات غير المحفوظة في محرر الملفات

صار محرر البرمجة يميّز المسودّة عن آخر محتوى محفوظ لكل ملف. قبل فتح ملف آخر أو تبديل المشروع أو إغلاق تبويب الملف أو إنشاء مشروع جديد، يفحص الحارس إن كانت المسودّة قد تغيّرت؛ فإن لم تتغير ينتقل فوراً، وإن تغيّرت يطلب تأكيداً صريحاً يذكر أن الانتقال سيهمل التعديلات. عند الإلغاء لا تتغير هوية الملف ولا محتوى المسودّة؛ وعند التأكيد يستمر الانتقال المعتاد. لا يزعم هذا الحارس أنه حفظ المحتوى تلقائياً، بل يحافظ على قرار المستخدم بين زر **«حفظ»** الفعلي والتخلي الصريح عن التعديل.

يبرهن اختبار `unsaved-file-guard.test.ts` الحالات التي لا تحتاج تأكيداً والحالات التي تحتاجه لمسارات الإغلاق وتبديل الملف والمشروع وإنشاء المشروع. اجتاز أيضاً `pnpm check`، وفاحص مساحة العمل الطرفي الذي يغطي إنشاء وحفظ وإعادة تسمية وحذف الملفات وعزل الحسابات مع تنظيف بيانات التحقق، ومجموعة Vitest الكاملة (**24 ملفاً و58 اختباراً**).

## حماية مسودات الشرائح

يستخدم استوديو العروض الآن قاعدة القرار نفسها لحماية مسودّة الشريحة. تقارن الواجهة **العنوان** و**المحتوى** و**ملاحظات المتحدث** بالحالة المحفوظة قبل تبديل الشريحة أو العرض، أو إضافة شريحة أو عرض، أو حذف أي منهما. عند وجود اختلاف، لا يجري الانتقال إلا بعد تأكيد صريح يبين أن المسودّة غير محفوظة؛ أما الإلغاء فيُبقي العرض والشريحة والمسودّة كما هي. لا تنفذ الواجهة حفظاً ضمنياً ولا تدّعي أن التوليد أو التصدير متاحان.

ثبت اختبار `unsaved-file-guard.test.ts` أن اختلاف أي من الحقول الثلاثة يستلزم التأكيد، وأن المسودّة المطابقة لا توقف الانتقال. اجتاز التغيير `pnpm check` وفاحص مساحة العمل الطرفي الذي يغطي دورة إنشاء وتعديل وإعادة ترتيب وحذف الشرائح والعروض، مع تنظيف بيانات التحقق. لم يُنشأ عرض مزيّف للفحص البصري؛ لأن هذا السلوك مغطى بعقد قرار نقي وفحص طرفي للخلفية.

## حماية مسودات العقل الثاني

يحمي قسم العقل الثاني الآن العنوان والمحتوى في مسودة الملاحظة قبل اختيار عنصر معرفي آخر أو إنشاء عنصر أو حذف العنصر المفتوح. إذا اختلف أحد الحقلين عن آخر حالة محفوظة، يطلب الانتقال تأكيداً صريحاً ولا يغير الملاحظة أو المسودة عند الإلغاء؛ أما التأكيد فيتابع العملية المقصودة من دون حفظ ضمني أو ادعاء حفظ تلقائي. تستعمل هذه الحماية قاعدة القرار المشتركة نفسها التي تستخدمها الملفات والشرائح، فلا يوجد منطق قرار متعارض بين الأقسام.

يغطي اختبار `unsaved-file-guard.test.ts` حالة العنصر المعرفي المطابق والمعدّل ومسارات التبديل والإنشاء والحذف. واجتاز `pnpm check` وفاحص `pnpm verify:workspace-e2e` الذي يثبت دورة CRUD للملاحظات والروابط واستخراج المهام، وعزل الحسابات، وتنظيف بيانات الاختبار. لم تُنشأ ملاحظات لغرض العرض المرئي؛ فالتغيير حماية محلية لمسودة المستخدم وتغطيه الاختبارات القابلة للتكرار.

## لوحة مهام مساحة العمل

أضيفت لوحة مهام إلى لوحة التحكم لعرض المهام التي يملكها الحساب الحالي فقط، مع وصف واضح بأن النقر على الحالة يغيّرها وأن الحذف يزيلها من الخادم. ترتبط القائمة بإجراءات `workspace.task.list` و`workspace.task.update` و`workspace.task.delete` ولا تحتفظ ببيانات نموذجية محلية. تغطي الواجهة التحميل والخطأ والفراغ، وتعطّل الإجراء الجاري فقط حتى لا تتظاهر بأن التحديث أو الحذف اكتمل قبل رد الخادم.

في الفحص المرئي المحلي بتاريخ 26 أغسطس 2026، ظهرت اللوحة العربية ضمن صفحة لوحة التحكم بحالة فراغ صادقة: **«لا توجد مهام محفوظة. ستظهر المهام التي تنشئها أو تستخرجها هنا.»**، مع رابط فعلي إلى قسم البرمجة. لم تُنشأ مهمة مخصصة للعرض؛ لأن فاحص مساحة العمل الطرفي أنشأ وحدّث وحذف المهام في بيئة الحساب المعزولة ثم نظفها. كما أظهر فحص DOM بعد استقرار الطلب أن حقل النموذج كان مفعلاً ونصه **«اختر نموذج OpenCode»**، ما يثبت أن لقطة التحميل المبكرة لا تعني تعطيلاً أو قائمة ثابتة.

## إنشاء المهام من لوحة التحكم

أضيف نموذج إنشاء داخل لوحة المهام نفسها. يرسل النموذج العنوان المنقح وحالة `todo` فقط إلى عقد `workspace.task.create`، ويحوّل اختيار **«مهمة عامة»** صراحةً إلى `projectId: null`؛ أما اختيار مشروع محفوظ فيرسل معرّفه فقط. لا يرسل العميل مالكاً أو صفاً محلياً متفائلاً، لأن الملكية تُشتق في الخادم من الحساب المصادَق عليه وتظهر المهمة بعد إبطال استعلام القائمة وسجل النشاط وإعادة جلبهما.

| الحالة | السلوك المتحقق |
|---|---|
| عنوان فارغ أو مسافات فقط | يبقى زر **«إضافة مهمة»** معطلاً؛ لا يمكن إرسال طلب فارغ. |
| مهمة عامة | يظهر الخيار العربي **«مهمة عامة»** وهو الاختيار الافتراضي، ولا يتطلب وجود مشروع محفوظ. |
| حفظ جارٍ | يتعطل زر الإرسال فقط ويعرض **«يجري الحفظ…»** حتى يصل رد العقد. |
| نجاح أو فشل | تظهر رسالة محلية صادقة؛ عند النجاح تمسح الحقول وتُحدَّث القائمة من الخادم، وعند الفشل لا تُضاف مهمة إلى الواجهة. |

أثبت الاختبار `client/src/lib/workspace-task-create.test.ts` أن المهمة ذات العنوان الصالح مقبولة من دون مشروع، وأن العنوان الفارغ أو الإرسال المتزامن مرفوضان. واجتازت الدفعة `pnpm check`، وفاحص `pnpm verify:workspace-e2e` الذي يغطي إنشاء وتحديث وحذف المهام مع عزل الحسابات وتنظيف بيانات الاختبار، ومجموعة Vitest الكاملة (**26 ملف اختبار و64 اختباراً**) وفحص حدود التكامل الذي أعاد `runtimeReferenceCount: 0` لـ OmniRoute. في مراجعة سطح المكتب العربية ظهر الحقل وخيار **«مهمة عامة»** والزر المعطل قبل إدخال عنوان، من دون إنشاء مهمة مخصصة للعرض.

## تحرير عناوين المهام من لوحة التحكم

أصبح لكل مهمة محفوظة زر **«تحرير»** يفتح حقلاً ضمن صف المهمة نفسه ويحمل العنوان الموثوق من القائمة. لا يحفظ الحقل إلا عنواناً منقحاً غير فارغ ومختلفاً عن الأصل، وبحد أقصى 240 حرفاً، ثم يرسل `id` و`title` فقط إلى عقد `workspace.task.update`. يحافظ الخادم على اشتقاق الملكية من الحساب المصادَق عليه، ولا يتغير أي صف في الواجهة تفاؤلياً؛ بل يُغلق وضع التحرير ويُحدَّث العنوان بعد إبطال قائمة المهام وسجل النشاط عند نجاح الاستجابة.

| الحالة | السلوك المتحقق |
|---|---|
| العنوان فارغ أو لم يتغير | زر **«حفظ»** معطل، ولا يرسل النموذج طلب تحديث. |
| حفظ جارٍ | تتعطل حالة المهمة وحذفها وأزرار التحرير الخاصة بها لمنع التعارض؛ زر الإلغاء أيضاً معطل حتى انتهاء الطلب. |
| حفظ ناجح | تظهر رسالة نجاح صادقة وتعيد الواجهة جلب القائمة والنشاط من الخادم. |
| فشل الحفظ | تبقى بيانات الخادم المعروضة دون تغيير تفاؤلي وتظهر رسالة خطأ قابلة للقراءة. |

اختبر `client/src/lib/workspace-task-title-edit.test.ts` لقبول التغيير غير الفارغ ورفض العنوان الفارغ أو غير المتغير أو المحفوظ بالتزامن. كما اجتاز `pnpm check`، وفاحص دورة مساحة العمل الذي يتضمن `task-create-update-delete` مع تنظيف البيانات، ومجموعة الاختبارات كاملة (**27 ملفاً و66 اختباراً**)، وفحص الحدود الذي أكد `runtimeReferenceCount: 0` لـ OmniRoute. راجعت لقطة سطح المكتب العربية النموذج ولوحة المهام على القماش المكتبي من دون إنشاء مهمة اختبار خاصة.

## تاريخ استحقاق المهمة

يتوفر لكل مهمة محفوظة زر **«تاريخ»** يفتح حقل تاريخ ضمن صفها. يحمّل الحقل تاريخ الاستحقاق المخزن، أو يبقى فارغاً حين لا يوجد تاريخ، ويمكن للمستخدم اختيار يوم جديد أو مسح القيمة ثم حفظها. يحوّل العميل قيمة حقل التاريخ إلى بداية اليوم في المنطقة الزمنية المحلية قبل إرسال كائن `Date` إلى عقد `workspace.task.update`؛ ومن ثم تنقلها طبقة العقد كقيمة زمنية معيارية إلى الخادم. لا يرسل العميل معرّف مالك ولا يعدّل الصف تفاؤلياً.

| الحالة | السلوك المتحقق |
|---|---|
| تاريخ صالح مختلف | يُفعّل زر **«حفظ»** ويرسل `id` و`dueAt` فقط إلى عقد التحديث المملوك. |
| مسح تاريخ موجود | يمكّن زر **«مسح»** جعل الحقل فارغاً، ثم يحفظ `dueAt: null` بعد تأكيد المستخدم بزر الحفظ. |
| تاريخ غير صالح أو غير متغير | يبقى الحفظ معطلاً، فلا يُرسل تحديث فارغ أو مكرر. |
| حفظ جارٍ أو تحرير مفتوح | تتعطل إجراءات الحالة والحذف والتحرير المتعارضة لنفس المهمة، ولا تتحرر الواجهة إلا بعد رد العقد. |

يعرض الصف التاريخ المحفوظ بلغة الواجهة الحالية باستخدام تنسيق تاريخ محلي من دون تغيير قيمته المخزنة. وبعد النجاح تبطل الواجهة قائمة المهام وسجل النشاط، فتظهر القيمة التي أعادها الخادم فقط؛ ويعرض الفشل رسالة صادقة من دون قيمة مؤقتة. يثبت `workspace-task-due-date.test.ts` تحويل اليوم المحلي، وصلاحية الحفظ والتفريغ، ورفض التاريخ غير الصالح أو غير المتغير أو الحفظ المتزامن. اجتازت الدفعة `pnpm check`، وفاحص `pnpm verify:workspace-e2e` الذي يثبت `task-create-update-delete` مع التنظيف، ومجموعة Vitest الكاملة (**28 ملفاً و68 اختباراً**) وفحص الحدود الذي أبقى `runtimeReferenceCount: 0` لـ OmniRoute. راجعت لقطة لوحة التحكم على القماش المكتبي من دون إضافة مهمة عرض مصطنعة؛ لذلك لم يُدّعَ فحص بصري لتاريخ على مهمة غير موجودة.

## وصف المهمة

أضيف حقل **«الوصف (اختياري)»** إلى نموذج إنشاء المهمة، ويرسل النص المنقح فقط بوصفه `description` إلى عقد `workspace.task.create`، أو يرسل `null` عندما يترك المستخدم الحقل فارغاً. يحتفظ النموذج بقاعدة العنوان الإلزامي ولا ينشئ صفاً محلياً؛ وبعد نجاح العقد يمسح العنوان والوصف واختيار المشروع ثم يعيد جلب قائمة المهام وسجل النشاط.

في كل صف محفوظ، يفتح زر **«وصف»** محرر نص متعدد الأسطر. يسمح المحرر بإضافة وصف أو تغييره أو مسحه، لكنه لا يرسل طلباً إذا لم تتغير النسخة المنقحة عن القيمة المخزنة. عند الحفظ يرسل العميل `id` و`description` فقط إلى `workspace.task.update`، وتُستمد الملكية خادمياً من الجلسة. يعطّل الصف الإجراءات المتعارضة أثناء الحفظ ولا يعرض نتيجة متفائلة؛ وتظهر القيمة الجديدة فقط بعد إبطال الاستعلامين وإعادة الجلب.

| الحالة | السلوك المتحقق |
|---|---|
| وصف جديد لمهمة بلا وصف | يصبح زر الحفظ متاحاً ويرسل النص المنقح إلى عقد التحديث. |
| مسح وصف محفوظ | يمسح زر **«مسح»** المسودة، ثم يحفظ العميل `description: null` عند الإرسال الصريح. |
| وصف مكافئ بعد التشذيب | يبقى الحفظ معطلاً ولا يصدر طلب مكرر. |
| حفظ جارٍ أو محرر آخر مفتوح | تتعطل الحالة والحذف وتحرير العنوان أو التاريخ المتعارض لنفس المهمة. |

يثبت `workspace-task-description.test.ts` إضافة وصف ومسحه ورفض قيمة مكافئة أو طلب متزامن. واجتازت الدفعة `pnpm check`، وفاحص `pnpm verify:workspace-e2e` الذي يغطي دورة المهام ويُنظف بيانات الاختبار، ومجموعة Vitest الكاملة (**29 ملفاً و71 اختباراً**) وفحص الحدود الذي أبقى `runtimeReferenceCount: 0` لـ OmniRoute. سجلت المراجعة البصرية في `docs/VISUAL-VERIFICATION-2026-08-26.md`: لم تنشئ الجلسة مهمة عرض، ولذلك لم يُدّعَ تحقق بصري لمحرر وصف داخل صف؛ كما وثقت الحاجة إلى تدقيق تخطيط مستقل للقماش المكتبي العريض.

## مشروع المهمة وإعادة الإسناد

بات كل صف مهمة محفوظة يعرض مشروعه الحالي، أو عبارة **«مهمة عامة»** إذا لم يكن له مشروع. يفتح زر **«مشروع»** قائمة اختيار ضمن الصف نفسها؛ وتضم خياراً صريحاً للمهمة العامة، ثم المشاريع التي أعاد عقد `workspace.project.list` إرجاعها للحساب. لا ينشئ العميل أسماء مشاريع أو معرّفات محلية ولا يرسل مالكاً مع التحديث.

عند تغيير الاختيار، يحوّل العميل القيمة الفارغة إلى `projectId: null` أو يرسل معرّف المشروع المحدد إلى `workspace.task.update`. يمنع الحارس الحفظ إذا بقي الاختيار كما هو أو كانت قيمته غير صالحة، ويعطّل إجراءات الصف المتعارضة أثناء الحفظ. تظهر النتيجة بعد إبطال قائمة المهام والنشاط وإعادة جلبهما؛ لذلك لا تظهر علاقة مشروع مؤقتة أو متفائلة في الواجهة.

| الحالة | السلوك المتحقق |
|---|---|
| تعيين مشروع مملوك | يرسل المحرر معرّف مشروع محدداً فقط، ويظهر الاسم الذي تعيده قائمة مشاريع الحساب بعد التحديث. |
| تحويل المهمة إلى عامة | يرسل المحرر `projectId: null` عند اختيار **«مهمة عامة»**. |
| اختيار غير متغير أو غير صالح | يبقى زر الحفظ معطلاً ولا يرسل تحديثاً. |
| حفظ جارٍ أو محرر آخر مفتوح | تتعطل حالة المهمة وحذفها ومحررات العنوان والوصف والتاريخ المتعارضة لنفس الصف. |

يثبت `workspace-task-project.test.ts` مساري التعيين والإلغاء ورفض القيم غير الصالحة أو غير المتغيرة أو الحفظ المتزامن. وتوسّع فاحص مساحة العمل المعزول بمسار `task-project-reassign-clear`، الذي ينشئ مشروعاً مملوكاً، يعيد إسناد مهمة إليه، يلغي الإسناد، ثم ينظف البيانات. اجتازت الدفعة `pnpm check`، و`pnpm verify:workspace-e2e`، ومجموعة Vitest الكاملة (**30 ملف اختبار و74 اختباراً**)، و`pnpm verify:integration-boundary` الذي أبقى `runtimeReferenceCount: 0` لـ OmniRoute، و`git diff --check`. راجعت لقطة سطح المكتب من دون إنشاء بيانات عرض؛ لذلك لم يُدّعَ تحقق بصري لمحدد مشروع داخل مهمة غير موجودة.

## تصفية حالة المهام

تعرض لوحة المهام مرشح حالات محلياً بعد نجاح استعلام `workspace.task.list`. الخيارات هي **«الكل»**، و**«قيد التنفيذ»**، و**«قيد العمل»**، و**«مكتملة»**؛ وهي لا تُنشئ سجلات ولا تعدّلها ولا تغير الاستعلام الخادمي. يطبق المساعد النقي المرشح على السجلات التي أعادها الخادم فقط، ولذلك تبقى قائمة الأصل والملكية وحالات الفشل والتحميل كما هي.

| الحالة | السلوك المتحقق |
|---|---|
| استعلام فارغ فعلياً | تعرض اللوحة حالة غياب المهام الأصلية، ولا تظهر أزرار مرشح لا يمكن تطبيقها. |
| قائمة جاهزة مع مرشح «الكل» | تظهر النتائج الخادمية الحالية كما هي، بحد العرض الموجود في لوحة المهام. |
| مرشح حالة يطابق سجلات | تظهر السجلات المطابقة فقط من النتائج المحمّلة بالفعل. |
| مرشح حالة بلا تطابق | تظهر رسالة **«لا توجد مهام بهذه الحالة»** بدلاً من الادعاء بأن الحساب لا يملك مهاماً. |

يثبت اختبار `workspace-tasks.test.ts` أن المرشح لا يصنع عناصر عميل، وأنه يميز الفراغ المصفى عن قائمة فارغة من الخادم. اجتازت الدفعة `pnpm check`، و`pnpm verify:workspace-e2e` مع تنظيف محتوى حساب الاختبار، ومجموعة Vitest الكاملة (**30 ملف اختبار و75 اختباراً**)، و`pnpm verify:integration-boundary` الذي أبقى `runtimeReferenceCount: 0` لـ OmniRoute، و`git diff --check`. في لقطة سطح المكتب لم توجد مهمة محفوظة لعرض أزرار المرشح؛ لذا لم يُدّعَ فحص بصري لتفاعل مرشح على بيانات مصطنعة.

## بيانات المشروع عند الإنشاء

استُبدل إنشاء المشروع عبر نافذة المتصفح المؤقتة بنموذج مضمّن في مستكشف البرمجة. يجمع النموذج الاسم الإلزامي واللغة والوصف الاختياريين، ثم يرسل القيم المنقحة فقط إلى عقد `workspace.project.create`. لا يحمل الطلب معرف مالك، ولا ينشئ صفاً محلياً متفائلاً؛ فالملكية والقيم المرجعة يحددهما الخادم المصادَق عليه. بعد النجاح يختار العميل المشروع الذي أعاده العقد، ويمسح المسودة ويغلق النموذج، ثم يبطل استعلامات المشاريع والملفات والمهام والنشاط حتى تصبح الواجهة مبنية على البيانات الخادمية الجديدة.

| الحالة | السلوك المتحقق |
|---|---|
| اسم فارغ أو مسافات فقط | يبقى زر **«إنشاء المشروع»** معطلاً، ولا يُرسل طلب إنشاء. |
| لغة أو وصف فارغان | تتحول القيمة المنقحة إلى `null`، فلا تُحفظ سلسلة فارغة بوصفها بيانات مشروع. |
| حدود العقد | يرفض الحارس الاسم الأطول من 160 حرفاً، واللغة الأطول من 64، والوصف الأطول من 100000؛ وتبقى العملية محلية قبل طلب الخادم. |
| حفظ جارٍ | يمنع الحارس إرسالات متزامنة ويعطل زري الحفظ والإلغاء حتى يصل رد العقد. |
| نجاح أو فشل | عند النجاح تظهر القيمة الخادمية فقط بعد الإبطال وإعادة الجلب؛ وعند الخطأ يبقى النموذج بلا مشروع محلي مصطنع وتُعرض رسالة العقد الفعلية. |

يثبت `client/src/lib/workspace-project-create.test.ts` تشذيب الحقول، وتحويل الاختياري الفارغ إلى `null`، وحدود العقد، ومنع الإرسال المتزامن. وتوسع `pnpm verify:workspace-e2e` ليتحقق من بقاء `language` و`description` في المشروع الذي ينشئه فاحص CRUD ثم ينظفه في النجاح والفشل. كما اجتازت الدفعة `pnpm check`، ومجموعة `pnpm test` الكاملة، و`pnpm verify:integration-boundary` الذي أبقى `runtimeReferenceCount: 0` لـ OmniRoute، و`git diff --check`.

راجعت الجلسة العربية الحية نموذج المستكشف من دون إنشاء مشروع: ظهرت الحقول الثلاثة، وتحقق DOM من تعطيل زر الإنشاء عندما يكون الاسم فارغاً. استُخدم الإلغاء بعد ذلك، وأثبت الفحص بقاء القائمة على «أنشئ مشروعاً للبدء» فقط. يسجل `docs/VISUAL-VERIFICATION-2026-08-26.md` هذا الدليل وحدود الفحص؛ لذلك لا يُدّعى تحقق مرئي لمسار حفظ مشروع اصطناعي.

## تحرير بيانات المشروع

استُبدل مسار إعادة تسمية المشروع عبر `window.prompt` بنموذج مضمّن لتحرير الاسم واللغة والوصف للمشروع المملوك. تُعبأ المسودات من المشروع المحدد الذي أعاده استعلام المشاريع، ثم يقرر الحارس النقي ما إذا كان الطلب صالحاً ومختلفاً عن القيم الخادمية قبل إرسال `workspace.project.update`. لا يرسل العميل حقولاً زائدة أو معرف مالك، ولا يغيّر القائمة محلياً قبل الرد؛ يبقى الخادم هو مصدر القيم والملكية.

| الحالة | السلوك المتحقق |
|---|---|
| لا تغييرات | يبقى زر **«حفظ التغييرات»** معطلاً ولا يُرسل طلب تحديث. |
| اسم غير صالح أو متجاوز للحد | يمنع الحارس الإرسال قبل الشبكة؛ الحدود هي 1–160 للاسم، و64 للغة، و100000 للوصف. |
| مسح اللغة أو الوصف | يحوّل الحارس القيمة المنقحة الفارغة إلى `null`، فتُمسح القيمة الاختيارية عبر العقد بدلاً من حفظ سلسلة فارغة. |
| حفظ جارٍ | يمنع الحارس الإرسال المتزامن وتُعطّل إجراءات التحرير المتعارضة حتى يصل رد العقد. |
| نجاح الخادم | يختار العميل المشروع المعاد، ويغلق النموذج ويمسح مسودته، ثم يبطل استعلامات المشاريع والملفات والمهام والنشاط؛ لذلك لا توجد نتيجة متفائلة مصطنعة. |

يثبت `client/src/lib/workspace-project-update.test.ts` تشذيب الحقول، وحدود العقد، وتحويل الاختياري إلى `null`، وحظر الحفظ غير المتغير أو المتزامن. كما توسع `pnpm verify:workspace-e2e` ليؤكد حفظ الاسم واللغة والوصف بعد تحديث المشروع، مع استمرار فحوص عزل الملكية والتنظيف. اجتازت الدفعة `pnpm check`، واختبار الحارس المنفرد، وفاحص CRUD، و`pnpm verify:integration-boundary` مع بقاء `runtimeReferenceCount: 0` لـ OmniRoute. وتؤكد المجموعة المحلية المستقلة `pnpm vitest run --exclude server/presenton/embeddedRuntime.test.ts` نجاح **31 ملف اختبار و82 اختباراً**.

تعذر `pnpm test` الكامل بسبب اختبار صحة Presenton الخارجي: خدمة الحلقة المحلية لم تستجب في وقت الفحص، وهو نفس الشرط الذي يفحصه الاختبار ولا صلة له بعقد مشروع أو واجهته. لم يُحوَّل ذلك إلى حالة نجاح ولم تُدّعَ قابلية توليد أو تصدير Presenton؛ فُصل العائق عن الدفعة، وأثبتت المجموعة المستبعدة منه سلامة بقية الاختبارات. اجتاز `git diff --check` قبل التوثيق، ويعاد بعده قبل الالتزام.

## إنشاء الملفات وإعادة تسميتها

استُبدلت نافذتا `window.prompt` لإنشاء الملف وإعادة تسميته بنموذجين مضمنين في مستكشف البرمجة. نموذج الإنشاء لا يفتح بلا مشروع مملوك محدد، ويجمع المسار المطلوب واللغة الاختيارية ثم يرسل حمولة منقحة إلى `workspace.file.create`. أما نموذج إعادة التسمية فيبدأ من مسار الملف الخادمي الحالي ويرسل المسار المنقح فقط إلى `workspace.file.rename`. لا يضيف أي مسار معرف مالك أو ملفاً محلياً متفائلاً؛ فالملف النهائي وملكيته يأتيان من العقد المحمي.

| الحالة | السلوك المتحقق |
|---|---|
| لا يوجد مشروع محدد | تُحجب عملية إنشاء الملف بسبب صادق؛ لا يظهر نموذج لا يمكن لعقده العمل ولا يُرسل طلب. |
| مسار فارغ أو غير صالح | يتعطل الإرسال محلياً قبل الشبكة، ويتبع الحارس قواعد تطبيع المسار الخادمية والحدود الفعلية. |
| لغة فارغة | تتحول إلى `null` في حمولة الإنشاء بدلاً من حفظ سلسلة فارغة. |
| إعادة تسمية بلا تغيير | يبقى حفظ الاسم الجديد معطلاً ولا يُرسل تحديث وهمي. |
| حفظ جارٍ | يمنع الحارسان الإرسال المتزامن ويعطلان الإجراءات المتعارضة حتى يعود العقد. |
| نجاح العقد | يغلق النموذج وتُمسح مسودته، ثم تبطل الواجهة استعلامات ملفات المشروع والنشاط وتعيد فتح الملف المرجع من الخادم فقط. |

يغطي `client/src/lib/workspace-file-form.test.ts` تطبيع المسار واللغة، وحدود الحقول، وتحويل اللغة الفارغة إلى `null`، ومنع إنشاء أو إعادة تسمية متزامنين أو غير متغيرين. كما وسع `pnpm verify:workspace-e2e` دليل CRUD للملفات ليتحقق من اللغة والمحتوى الابتدائيين بعد الإنشاء، ثم الحفظ وإعادة التسمية والحذف والعزل والتنظيف. اجتازت الدفعة `pnpm check` واختبارات الحارسين وفاحص CRUD وفاحص حدود التكامل مع بقاء `runtimeReferenceCount: 0` لـ OmniRoute.

تعذر `pnpm test` الكامل للسبب التشغيلي الخارجي نفسه الخاص باختبار صحة Presenton غير المستجيب. لم يدخل أي من حارسي الملف أو نموذجَيه في ذلك العائق، واجتازت مجموعة Vitest المستبعدة من اختبار Presenton بقية الاختبارات. لا يُستنتج من ذلك أن Presenton صالح للتوليد أو التصدير. سجّل `docs/VISUAL-VERIFICATION-2026-08-26.md` فحص النموذجين في متصفح حي وتنظيف مشروع وملف المراجعة المؤقتين؛ ويعاد فحص `git diff --check` بعد هذا التوثيق قبل الالتزام.
