# حالة رفع GitHub

## الدفعة المحلية

| البند | القيمة |
|---|---|
| الالتزامات المحلية غير المدفوعة | `8cdddccb` — `feat: integrate verified OpenCode sessions`؛ ثم `39e1a337` — `feat: clarify OpenCode model selection and help states` |
| المحتوى | تكامل جلسات OpenCode المتحقق، سياسة تشغيل موحدة، سياق آمن للأقسام الثلاثة، لوحة أوامر حقيقية، فاحصات جلسة/دور/مهلة، ثم مركز مساعدة حي، وتصحيح اختيار النموذج بلا قيمة افتراضية، وتوثيق التشغيل. |
| فحص ما قبل الالتزام | نجح `git diff --check`، و`pnpm check`، و`pnpm test` (51 اختباراً)، وفاحصات مساحة العمل وحدود التكامل وOpenCode الحية. |

## عائق الرفع

في محاولة الدفع إلى `github/main` فشل Git على طبقة المصادقة برسالة `Invalid username or token`. أُعيد فحص `gh auth status` في 26 أغسطس 2026؛ ويؤكد أن الرمز الفعال في `GH_TOKEN` وبيانات اعتماد GitHub CLI المحليين غير صالحين. لم تُجرَ محاولة دفع عمياء جديدة. كما أن صفحة المستودع العامة الظاهرة في المتصفح غير مسجّل الدخول فيها، وآخر التزام بعيد ظاهر هو `30cca25`.

> لا توجد إعادة محاولة عمياء أو تغيير لبيانات الاعتماد. يبقى الالتزام المحلي محفوظاً وجاهزاً للدفع فور تجدد رمز GitHub أو جلسة موثوقة.
